# 포트폴리오 리뉴얼 작업 정리

## 현재 작업 브랜치

- 브랜치: `codex/renewal`
- 아직 커밋하지 않은 변경사항이 있습니다.

## 주요 변경사항

### 1. 파일 구조 정리

- 기존 `src/public` 이미지 경로를 `src/image`로 변경했습니다.
- 기존 CSS/JS 경로를 `src/css` 하위로 정리했습니다.
- `index.html`에서 참조 경로를 아래처럼 변경했습니다.

```html
<link rel="stylesheet" href="./src/css/global.css">
<link rel="stylesheet" href="./src/css/common.css">
<link rel="stylesheet" href="./src/css/style.css">
<script src="./src/css/script.js"></script>
```

### 2. 폰트 적용

- `src/fonts` 안의 Pretendard 폰트를 사용하도록 변경했습니다.
- `global.css`에서 `@font-face`로 로컬 폰트를 연결했습니다.
- 기본 폰트 기준은 `16px`이며, CSS 단위는 `rem` 중심으로 정리했습니다.

### 3. 내비게이션 추가

- 상단 내비게이션을 추가했습니다.
- 로고 텍스트는 `Young Hyun`으로 변경했습니다.
- 메뉴는 `Intro`, `Skills`, `Portfolio`로 구성했습니다.
- 모바일에서는 로고가 보이지 않도록 처리했습니다.

### 4. 퍼블리셔 역량 섹션 추가

- 인트로와 포트폴리오 사이에 `퍼블리셔로서의 역량` 섹션을 추가했습니다.
- 구성은 아래 4개 카드입니다.

- `01 마크업 & 구조`
- `02 반응형 & 크로스브라우징`
- `03 인터랙션 구현`
- `04 협업 & 유지보수`

주의: 이 섹션은 사용자가 "건들이지 말라"고 한 영역입니다. 이후 작업 시 임의 수정하지 않는 것이 좋습니다.

### 5. 포트폴리오 헤더 수정

- 포트폴리오 상단에 아래 요소를 추가했습니다.

- `SELECTED WORK · 2022-2026`
- `Portfolio`
- `6 Projects`
- 설명 문구

현재 프로젝트 개수는 6개 기준입니다.

### 6. 프로젝트 카드 레이아웃 변경

- 기존 프로젝트 상세 영역을 카드형 구성으로 변경했습니다.
- 공통 구조는 아래 순서입니다.

- 프로젝트 배지
- 프로젝트 상세 제목
- 한 줄 설명
- 담당 역할 또는 사용 기술
- 주요 개발내용 토글
- 기술 개선 포인트 토글
- 사이트 방문하기 CTA

### 7. 프로젝트 리스트 상태

현재 포트폴리오 프로젝트는 6개입니다.

- `01 GSAP 인터랙티브 포트폴리오`
- `02 하나 투어`
- `03 네이버 개발자 센터`
- `04 티파니앤코`
- `05 Tving 티빙`
- `06 퍼블리싱 컨벤션 + 가이드`

단, 사용자가 요청한 대로 `02`, `04`, `06`은 상단 썸네일 제목 영역의 텍스트만 삭제하고 숫자만 남겨두었습니다.

상세 카드 안의 프로젝트 제목과 내용은 유지되어 있습니다.

### 8. 토글 버튼 개선

- 주요 개발내용 / 기술 개선 포인트는 토글 구조를 유지했습니다.
- `+` 버튼 클릭 시 transition을 사용해 `x` 형태로 바뀌도록 CSS를 수정했습니다.

### 9. CSS 정리

- 컬러, 간격, 타이포 등 일부 값을 CSS 변수 기반으로 정리했습니다.
- 기존 `px` 단위를 `rem`으로 변경했습니다.
- `style.css`를 섹션별로 나누어 관리하기 쉬운 구조로 정리했습니다.

## 주의사항

- `src/public` 파일들은 git 기준으로 삭제 상태이고, 새 파일들은 `src/image`에 있습니다.
- 기존 `src/common.css`, `src/global.css`, `src/style.css`, `src/script.js`는 삭제 상태이며, 새 위치는 `src/css`입니다.
- 현재 `script.js` 위치가 `src/css/script.js`입니다. 구조상 어색하면 추후 `src/js/script.js`로 분리하는 것을 추천합니다.
- 포트폴리오 카드 복구 과정에서 `02`, `04`, `06`의 상단 제목 텍스트는 의도적으로 비워두었습니다.

## 다음 확인하면 좋은 것

- 브라우저에서 6개 프로젝트 카드가 모두 정상 출력되는지 확인
- 모바일에서 `Young Hyun` 로고가 숨김 처리되는지 확인
- 토글 버튼 클릭 시 `+`에서 `x`로 자연스럽게 전환되는지 확인
- 이미지 경로가 모두 `src/image` 기준으로 정상 로드되는지 확인
- 최종 확인 후 커밋 및 푸시 진행
